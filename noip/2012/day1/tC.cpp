#include <bits/stdc++.h>
#define ll long long
using namespace std;
const int N = 1e5 + 10;
int n;
ll a[N];

void init() {
  for(int i = 1; i <= n; ++i) {
    
  }
}

int main() {
  scanf("%d", &n);
  for(int i = 1; i <= n; ++i) {
    scanf("%lld", &a[i]);
  }
  init();
  return 0;
}