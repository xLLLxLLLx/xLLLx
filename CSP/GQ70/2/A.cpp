#include <bits/stdc++.h>
using namespace std;
const int N = 1010;
struct data {
  int nt, to;
} a[N];
int f[N], dis[N], cnt = 0;

int main() {
  int T;
  scanf("%d", &T);
  for(int o = 1; o <= T; ++o) {
    int n;
    scanf("%d", &n);
    for(int i = 1; i <= n; ++i) {
      scanf("%d", &s[i]);
    }

  }
  return 0;
}