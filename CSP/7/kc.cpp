#include <bits/stdc++.h>
using namespace std;
const int N = 110;
char s[N], t[N];

int main() {
  scanf("%s%s", t, s);
  int len = strlen(t), gg = strlen(s);
  printf("%d %d\n", len, gg);
  return 0;
}