#include <bits/stdc++.h>
using namespace std;

int main() {
	int T;
	scanf("%d", &T);
	for(int o = 1; o <= T; ++o) {
		int a, b, c;
		scanf("%d%d%d", &a, &b, &c);
		int n;
		for(int i = 1; i <= n; ++i) {
			int x, y, z;
			scanf("%d%d%d", &x, &y, &z);
			
		}
	}
	return 0;
}