#include<bits/stdc++.h>
#define fr(i,x,y) for(int i=x;i<=y;++i)
#define rf(i,x,y) for(int i=x;i>=y;--i)
#define ls q[x].ch[0]
#define rs q[x].ch[1]
#define LL long long
using namespace std;

int main(){
  srand(time(NULL));
  int x=100000;
  cout<<x<<endl;
  fr(i,1,x){
    int x=rand()%4000000000
  }
  return 0;
}
