#include<bits/stdc++.h>
#define fr(i,x,y) for(int i=x;i<=y;++i)
#define rf(i,x,y) for(int i=x;i>=y;--i)
#define LL long long 

int main(){
  int n;scanf("%d",&n);
  fr(i,1,n){
    int m;scanf("%d",&m);
    fr(i,1,m-1) {
      int x,y,z;
      scanf("%d%d%d",&x,&y,&z);
    }
    string ll;
    while(1){
      cin>>ll;
      if(ll=="D"
    }
  }
  return 0;
}
