#include<bits/stdc++.h>
using namespace std;
const int N = 110;
int a[N];

int main() {
	int n;
	scanf("%d", &n);
	for(int i = 1; i <= n; ++i) {
		scanf("%d", &a[i]);
	}
	for(int i = 1; i <= n; ++i) 
		for(int j = 1; j <= m; ++j) 
			for(int k = 1; k <= t; ++k) {
				int x=nmdp;
				printf("%d\n", x);
			}
	return 0;
}