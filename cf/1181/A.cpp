#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
	ll x, y, z;
	cin >> x >> y >> z;
	ll pos = x / z, tmp = y / z;
	if(x%z == 0) {
		printf("%lld %lld\n", pos, tmp);
	} else if(y%z == 0) {
		printf("%lld %lld\n", pos, tmp);
	} else {
		it
	}
	return 0;
}