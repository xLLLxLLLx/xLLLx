#include<iostream>
#include<cstdio>
#include<algorithm>
#include<cstring>
#include<queue>
#include<vector>
#include<cmath>

#define fr(i,x,y) for(int i=x;i<=y;++i)
#define rf(i,x,y) for(int i=x;i>=y;--i)
#define ll long long
using namespace std;
const int N=1010;
char lx[N];

int main(){
	int n;scanf("%d",&n);
	fr(o,1,n){
		scanf("%s",lx+1);
		int len=strlen(lx+1);
		
	}
	return 0;
}