#include<bits/stdc++.h>
#define fr(i,x,y) for(int i=x;i<=y;++i)
#define rf(i,x,y) for(int i=x;i>=y;--i)
#define ll long long
using namespace std;
const int N=3e5+10;
char lx[N];

int main(){
	scanf("%s",lx+1);
	int len=strlen(lx+1);
	ll ans=0;
	fr(i,1,len) {
		int pos=lx[i]-'0';
		
	}
	printf("%lld\n",ans);
	return 0;
}