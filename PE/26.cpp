#include <bits/stdc++.h>
using namespace std;
const int N = 1010;
int nm[N], ans = 0;

void cut(int x) {
  memset(nm, 0, sizeof nm);
  int pos = 1;
  for(int i = 1; ; ++i) {

  }
}

int main() {
  for(int i = 1; i <= 1000; ++i) {
    cut(i);
  }
  printf("%d\n", ans);
  return 0;
}